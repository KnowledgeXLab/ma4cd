# Skill Template

Copy this directory to `skills/<your-skill-id>/` and fill in `manifest.yaml` + `rules/*.yaml`.

See [skills/README.md](../README.md) for the rule pack schema.

**Do not** set `MA4CD_SKILL=_template` — it is not a runnable instance.
